
<%= simple_form_for :search, method: "get" do |f| %>
  <%= f.input :starts_at, input_html: { id: "range_start" } %>
  <%= f.input :category, collection: Tour::CATEGORIES %>
  <%= f.button :submit, "Go", class: "btn btn-ghost" %>
<% end %>
